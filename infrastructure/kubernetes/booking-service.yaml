apiVersion: apps/v1
kind: Deployment
metadata:
  name: aerive-booking-service
  namespace: aerive
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aerive-booking-service
  template:
    metadata:
      labels:
        app: aerive-booking-service
    spec:
      containers:
      - name: booking-service
        image: 985121561706.dkr.ecr.us-east-1.amazonaws.com/aerive/booking-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3003
        envFrom:
        - configMapRef:
            name: aerive-config
        - secretRef:
            name: aerive-secrets
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3003
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3003
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: aerive-booking-service
  namespace: aerive
spec:
  selector:
    app: aerive-booking-service
  ports:
  - port: 3003
    targetPort: 3003
  type: ClusterIP

