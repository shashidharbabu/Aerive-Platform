apiVersion: apps/v1
kind: Deployment
metadata:
  name: aerive-redis
  namespace: aerive
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aerive-redis
  template:
    metadata:
      labels:
        app: aerive-redis
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          tcpSocket:
            port: 6379
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          tcpSocket:
            port: 6379
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: aerive-redis-service
  namespace: aerive
spec:
  selector:
    app: aerive-redis
  ports:
  - port: 6379
    targetPort: 6379
  type: ClusterIP

